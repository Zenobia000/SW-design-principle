SOLID設計原則筆記 - 長庚的作業簿

# SOLID設計原則筆記

[設計模式]-UML類圖的各符號含義

![alt text](image.png)
## 目錄
- [SOLID設計原則筆記](#solid設計原則筆記)
  - [目錄](#目錄)
  - [前言](#前言)
  - [OOP的四個特性](#oop的四個特性)
    - [抽象(Abstraction)](#抽象abstraction)
    - [封裝(Encapsulation)](#封裝encapsulation)
    - [繼承(Inheritance)](#繼承inheritance)
    - [多型(Polymorphism)](#多型polymorphism)
  - [內聚力與偶合力](#內聚力與偶合力)
    - [內聚力](#內聚力)
    - [耦合力](#耦合力)
    - [相依性](#相依性)
  - [原則](#原則)
    - [補充](#補充)
  - [學習SOLID設計原則的好處](#學習solid設計原則的好處)
  - [單一職責原則 Single Responsibility Principle (SRP)](#單一職責原則-single-responsibility-principle-srp)
  - [開放封閉原則 Open Closed Principle (OCP)](#開放封閉原則-open-closed-principle-ocp)
    - [OCP的時機](#ocp的時機)
  - [里氏替換原則 Liskov Substitution Principle (LSP)](#里氏替換原則-liskov-substitution-principle-lsp)
    - [LSP精神](#lsp精神)
  - [介面隔離原則 Interface Segregation Principle (ISP)](#介面隔離原則-interface-segregation-principle-isp)
    - [ISP時機](#isp時機)
  - [相依反轉原則 Dependency Inversion Principle (DIP)](#相依反轉原則-dependency-inversion-principle-dip)
    - [DIP的基本精神](#dip的基本精神)
    - [使用DIP的時機](#使用dip的時機)
  - [關於單元測試與SOLID](#關於單元測試與solid)

## 前言
開發的時間長？還是維護的時間長？  
是多人開發？還是一人開發？  
長期維護的專案，需求變更程度如何？一個月一次？一年一次？

## OOP的四個特性
### 抽象(Abstraction)
抽象是將真實世界的需求轉換成程是類別的過程(abstract可以翻譯成簡化、簡單化，"將真實世界的需求簡化成程式類別")，而類別可以包含屬性或方法，屬性為靜態狀態，方法為動態行為。

### 封裝(Encapsulation)
隱藏或保護類別內部實作的細節。

### 繼承(Inheritance)
當你要擴充或修改類別中定義的行為。可以使用繼承建立為新類別並保留類別原有的功能。但在覆寫時要記得只有virtual代表可以被子代複寫，執行時才會執行子代的方法。

### 多型(Polymorphism)
多個型別擁有相同的介面，或指某個型別擁有多個子型別。在C#所有類別全部都繼承自object，換句話說C#所有型別都是object的多型。

## 內聚力與偶合力
先了解什麼是模組(Model)。以C#來說，其實C#沒有Model這個概念，但我們談OOP的Model時，可以想成是C#中某個類別、方法或組件。

### 內聚力
內聚力是在一個模組內完成一件工作的度量指標。高內聚力意味著模組更容易獨立運作、更容易重複使用，例如一個類別只負責一件事；低內聚力則會造成難以維護/測試/重用/理解的問題。

### 耦合力
A模組與B模組相依性高，若改了B模組的內容，則A模組會受到影響，這是耦合力太高，導致改B壞A。使用DIP原則可降低耦合力。

高內聚與低耦合本質上互斥，理論上提高內聚力，就會把工作切成多個類別，而切成更多類別時，就意味著耦合度越高，所以現實上會依工作需求有很多考量。

### 相依性
有用到類別或介面就會產生相依，宣告什麼型別，new什麼物件都算相依。

## 原則
原則是一種概念或價值，用來導引你產生適切的行為與價值評量的方法。依循SOLID原則，可以寫出比較好的程式碼，並能夠判斷程式碼的好壞。

### 補充
Robert C. Martin整理了很多原則並濃縮提倡為物件導向設計原則，過程有增減與修改，於2004年以S、O、L、I、D順序排列，拼出SOLID設計原則。

## 學習SOLID設計原則的好處
1. 降低程式碼複雜度
2. 具有較佳的程式碼可讀性
3. 提升模組可重複利用性
4. 讓模組具有高內聚力，低耦合力
5. 當面臨需求變更時，可減少破壞現有模組的風險

## 單一職責原則 Single Responsibility Principle (SRP)
> 一個模組應該只有一個需要改變的理由。

以上是最初理念，隨著時間演變Martin將版本改為：
> 一個模組應該只對一個角色負責。

所以來說，一個類別如果負擔太多責任時，就意味著該類別可以被切割。

## 開放封閉原則 Open Closed Principle (OCP)
> 一個軟體製品應該對於擴展是開放的，但對於修改是封閉的。

應該藉由新增程式碼來擴充功能，而不是修改原本的程式碼。可以透過繼承來輕鬆擴充。

### OCP的時機
既有類別已經定義清楚，處於一個強調穩定的狀態，或是需要擴充現有類別，加入新需求的屬性或方法時。

## 里氏替換原則 Liskov Substitution Principle (LSP)
> 子類別可以被替換為其父類別。

保哥註釋：若你的程式碼有採用繼承或介面，在父型別出現的地方，都可以用子型別來取代，而不會破壞程式原有的行為。

### LSP精神
確實正確的實作繼承與多型。

## 介面隔離原則 Interface Segregation Principle (ISP)
> 客戶不應該強迫相依於沒有使用的方法。

多個給客戶專用的介面，優於一個通用需求介面。在ISP原則下，介面裡面有多個方法的話，應該拆成多個介面，每個介面就只有一點點的方法。

### ISP時機
當介面需要分割時，或當類別使用的時機也可以被分割時。

## 相依反轉原則 Dependency Inversion Principle (DIP)
> 高層次的模組不應該依賴於低層次的模組，兩者都應該依賴於抽象介面。

高接模組呼叫(new)低階模組，低階模組被高階呼叫。

### DIP的基本精神
所有類別都相依於抽象，而不是具體實作。

### 使用DIP的時機
想要降低耦合度時，希望類別都相依於抽象。

## 關於單元測試與SOLID
良好的SOLID設計有助於單元測試的實施，因為遵循SOLID原則的程式碼通常會更模組化且易於測試。





